/* import { assert } from "https://deno.land/std@v0.51.0/testing/asserts.ts";
import { tasklist } from "./mod.ts";

const { test } = Deno;

 test({
  name: "tasklist",
  async fn(): Promise<void> {
    let tasklistResult = await tasklist({});
    assert(tasklistResult.output);
  },
}); */

Deno.test("pass", () => {});
